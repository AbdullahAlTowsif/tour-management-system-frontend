import HeroSection from "@/components/modules/Homepage/HeroSection";

const Homepage = () => {
    return (
        <div>
            <HeroSection />
        </div>
    );
};

export default Homepage;
